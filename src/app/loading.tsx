export default function Loading() {
  return (
    <div className="fixed inset-0 z-[10000] bg-[#080A0F] flex items-center justify-center">
      <span className="text-[11px] tracking-[0.3em] uppercase text-white/50 font-medium animate-pulse">
        AIA Surveyors
      </span>
    </div>
  );
}
